DATABASE_URL="postgresql://secops:secops@localhost:5433/secops?schema=public"

# Exact origin of the frontend app — required for CORS to accept the
# httpOnly refresh-token cookie (credentials:true is incompatible with a
# wildcard origin).
FRONTEND_URL="http://localhost:3001"
